DST		= Declaration Statement
T		= Type
DS		= Declarations
D		= Decalration
ADS		= Additional Declaration Statements
VN		= Variable Name
EXP		= Expression


DSS		->	T DS			{
								DS.type = T.type;
							}

DS		->	D ADS			{
								D.type = DS.type = ADS.type;
							}

ADS		->	"," D ADS1		{
								D.type = ADS.type = ADS1.type;
							}

			| NULL			{}

D		-> 	VN				{	
								//Insert VN, d.type into symbol table
			| VN = EXP		{
								//insert VN, d.type into symbol table
								code += D.type + VN;
								code += VN = EXP.addr;
							}
T		->	Int
			| Char
			//similarly all types
								

